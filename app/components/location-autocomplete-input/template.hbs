<PowerSelect
  @options={{this.predictions}}
  @search={{perform this.requestPredictions}}
  @searchEnabled={{true}}
  @allowClear={{true}}
  @loadingMessage="Searching..."
  @selected={{this.selectedPlace}}
  @onChange={{on "findPlaceDetails"}} as |prediction|>
  {{prediction.description}}
</PowerSelect>
