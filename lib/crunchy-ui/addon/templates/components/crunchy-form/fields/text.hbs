{{#component group
  field=field
  model=model
  as |group|}}

  {{#if @label}}
    <group.label class={{@labelClass}}>
      {{@label}}
    </group.label>
  {{/if}}

  <group.control as |control|>
    <Input
      @id={{control.controlId}}
      @value={{this.value}}
      @type={{@type}}
      @placeholder={{@placeholder}}
      @disabled={{@disabled}}
      @focus-out={{action group.validateField @field}}
      aria-label={{@aria-label}}
      @autocomplete={{@autocomplete}}
      @data-test-id={{concat @field "-field"}}
      @class={{concat "crunchy-form__input "
        @controlClass
      }}
    />
  </group.control>

  {{#if group.hasError}}
    <group.errors class={{@errorClass}} />
  {{/if}}
{{/component}}
